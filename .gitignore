# --- Input data (per CLAUDE.md: data is not versioned) ---
*.osm.pbf
*.zip
*.gtfs
*.gpkg
*.gpkg-wal
*.gpkg-shm
*.csv

# --- OTP runtime artifacts ---
*.jar
Graph.obj
graphs/
router-config.json
build-config.json
*.log

# --- GTFS-RT artifacts / rt_diagnose tooling (never versioned) ---
*.pb
rt_archive/
recording.json
tools/rt_diagnose/.venv/

# --- ad hoc raw VehiclePositions snapshots (personal smoke-test data, not deliverable code) ---
tools/rt-lodz-feed-test/

# --- Family A reconstruction tooling (own venv, never versioned) ---
tools/family_a_reconstruction/.venv/
tools/family_a_reconstruction/*.csv
tools/family_a_reconstruction/*.parquet

# --- FA-10 manual verification scratch (raw GTFS-RT snapshots, downloaded static
# GTFS zips, ad hoc match/build outputs - personal test data, not deliverable code) ---
tools/family_a_reconstruction/gtfs-manual-test/

# --- transit_charts tooling (own venv - matplotlib must never reach family_a's, which is
# installed on the Termux phone; cached tidy tables and rendered figures are data) ---
tools/transit_charts/.venv/
tools/transit_charts/out/

# --- chart_lab tooling (own venv; build/dist are PyInstaller output, CL-6) ---
tools/chart_lab/.venv/
tools/chart_lab/build/
tools/chart_lab/dist/
tools/transit_charts/.pytest_cache/

# --- i18n developer tooling (build scripts, LLM translation helpers) ---
tools/i18n/
easy_otp.pro

# --- isochrones_lodz tooling (network_static/network_rt r5r build cache + copied
# inputs, intermediate ogr2ogr exports, staged data/ before manual copy into
# mapy-analizy/izochrony-lodz/data/ -- *.gpkg/*.csv/*.zip/*.osm.pbf already
# covered above, this adds the .geojson intermediates that aren't) ---
tools/isochrones_lodz/network_static/
tools/isochrones_lodz/network_rt/
tools/isochrones_lodz/*_isochrones_ogr.geojson
tools/isochrones_lodz/*_isochrones_simplified.geojson
tools/isochrones_lodz/data/
tools/isochrones_lodz/geobuf_pack/node_modules/

# --- Crash artifacts ---
bash.exe.stackdump

# --- Intermediate and output rasters ---
*.tif
*.tiff
*.vrt

# --- Working / cache directories ---
work/
workdir/
cache/
tmp/

# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
easy_otp/_vendor/

# --- QGIS artifacts ---
*.qgz
*.qgs
.qgis2/
*.qmd

# --- IDE / editors ---
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# --- Spike / research files (never commit credentials or raw API dumps) ---
api-key.txt
transitland-response.json
*.key
*.pub

# --- Development-only files (not part of the plugin distribution) ---
# Note: docs/ holds local-only specs/prompts; README images live in assets/.
docs/
papers/
reference/
CLAUDE.md
.claude/

# --- Build artifacts ---
easy_otp-package.zip

# --- tools/analysis generated outputs (data, not code — see CLAUDE.md .gitignore policy) ---
tools/analysis/*.png
tools/analysis/output/

# --- tools/accessibility_lodz: r5r build cache + machine-local Java pin (data, not code) ---
tools/accessibility_lodz/network_data/
tools/accessibility_lodz/rjavaenv/
tools/accessibility_lodz/.Rprofile
tools/accessibility_lodz/*.csv
tools/accessibility_lodz/run_accessibility.log

# --- tools/accessibility_cities: multi-city r5r pipeline (data, not code) ---
tools/accessibility_cities/pbf_regions/
tools/accessibility_cities/*/network.dat
tools/accessibility_cities/*/*.mapdb
tools/accessibility_cities/*/*.mapdb.p
tools/accessibility_cities/*/network_settings.json
tools/accessibility_cities/*/rows_voivodeship.json
tools/accessibility_cities/*/r5r-log.log
tools/accessibility_cities/*/gtfs_errors.csv
tools/accessibility_cities/*/*.log
tools/accessibility_cities/out/*.png
tools/accessibility_cities/*/out/*.png
